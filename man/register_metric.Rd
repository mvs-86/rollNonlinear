% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metrics-registry.R
\name{register_metric}
\alias{register_metric}
\title{Register a custom rolling metric}
\usage{
register_metric(name, fn, outputs, defaults = list(), min_len = NULL)
}
\arguments{
\item{name}{A single string. The metric name (key in the registry).}

\item{fn}{A function with formals \verb{(x, .args = list())} that accepts a
numeric vector \code{x} and a list of named arguments \code{.args}, and returns a
named numeric vector with names matching \code{outputs}.}

\item{outputs}{Non-empty, unique character vector of output column names
that \code{fn} returns.}

\item{defaults}{Named list of default arguments passed to \code{fn} as \code{.args}.
User-supplied \code{metric_args} override these via \code{\link[utils:modifyList]{utils::modifyList()}}.}

\item{min_len}{A function \verb{function(args) -> integer} that returns the
advisory minimum window size for the given resolved args. If \code{NULL},
defaults to \code{function(a) 2L}.}
}
\value{
Invisibly \code{NULL}. Called for side effects.
}
\description{
Adds a new metric (or replaces an existing one) in the package metric
registry. Once registered, the metric name is available for use in
\code{\link[=roll_nonlinear_one]{roll_nonlinear_one()}} and \code{\link[=roll_nonlinear]{roll_nonlinear()}}.
}
\examples{
my_fn <- function(x, .args = list()) c(my_mean = mean(x, na.rm = TRUE))
register_metric("my_mean", my_fn, outputs = "my_mean")
available_metrics()
# cleanup
rm("my_mean", envir = rollNonlinear:::.metric_registry)
}
