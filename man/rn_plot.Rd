% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{rn_plot}
\alias{rn_plot}
\title{Plot the output of \code{roll_nonlinear()}}
\usage{
rn_plot(
  result,
  metrics = NULL,
  type = c("time_series", "symbols", "distribution", "panel"),
  symbol_col = "symbol",
  time_col = "time",
  regime_thresholds = NULL,
  original_data = NULL,
  price_col = "close",
  facet_scales = "free_y",
  ncol = NULL,
  title = NULL
)
}
\arguments{
\item{result}{A \code{data.table} returned by \code{\link[=roll_nonlinear]{roll_nonlinear()}} or
\code{\link[=roll_nonlinear_one]{roll_nonlinear_one()}}.}

\item{metrics}{Character vector of metric column names to plot. \code{NULL}
(default) uses all metric columns detected in \code{result}.}

\item{type}{Display type: one of \code{"time_series"} (default), \code{"symbols"},
\code{"distribution"}, or \code{"panel"}.}

\item{symbol_col}{Name of the symbol column in \code{result}. Default: \code{"symbol"}.}

\item{time_col}{Name of the time column in \code{result}. Default: \code{"time"}.}

\item{regime_thresholds}{\code{NULL} (no overlay), \code{"auto"} (built-in thresholds
for \code{hurst} and \code{dfa_alpha}), or a named list of the form
\verb{list(metric_name = list(lo = <num>, hi = <num>))} for custom horizontal
reference bands.}

\item{original_data}{A \code{data.table} of the original OHLC data used to
produce \code{result}. Required when \code{type = "panel"}.}

\item{price_col}{Column name for the price series in \code{original_data}.
Default: \code{"close"}.}

\item{facet_scales}{Passed to \code{\link[ggplot2:facet_wrap]{ggplot2::facet_wrap()}} \code{scales} argument.
Default: \code{"free_y"}.}

\item{ncol}{Number of facet columns. \code{NULL} (default) lets ggplot2 decide.}

\item{title}{Optional plot title string.}
}
\value{
A \code{ggplot} object (\code{type \%in\% c("time_series","symbols","distribution")})
or a \code{patchwork} object (\code{type = "panel"}). Both can be further customised
with standard ggplot2 layers and themes.
}
\description{
Visualises rolling nonlinear metric results using \strong{ggplot2}. Four display
types are available:
}
\details{
\describe{
\item{\code{"time_series"}}{One line per symbol over time, faceted by metric
when multiple metrics are requested.}
\item{\code{"symbols"}}{Single metric across all symbols, each symbol in its
own facet panel.}
\item{\code{"distribution"}}{Histogram of metric values per symbol, faceted by
metric when multiple metrics are requested.}
\item{\code{"panel"}}{Price series on top + metric(s) below in a stacked
layout (requires \strong{patchwork} and \code{original_data}).}
}
}
\examples{
\donttest{
library(data.table)

set.seed(42L)
n <- 200L
dt <- data.table(
  symbol = rep(c("A", "B"), each = n),
  time   = c(
    seq(as.POSIXct("2024-01-02 09:30:00", tz = "UTC"), by = 60, length.out = n),
    seq(as.POSIXct("2024-01-02 09:30:00", tz = "UTC"), by = 60, length.out = n)
  ),
  close  = c(cumsum(rnorm(n)) + 100, cumsum(rnorm(n)) + 200)
)

result <- roll_nonlinear(dt, metrics = c("hurst", "dfa"),
                         window_size = 64L, stride = 8L)

# Time-series of both metrics, one line per symbol
rn_plot(result, type = "time_series")

# Regime threshold overlay (built-in for hurst / dfa_alpha)
rn_plot(result, metrics = "hurst", type = "time_series",
        regime_thresholds = "auto")

# All symbols in separate facets for a single metric
rn_plot(result, metrics = "hurst", type = "symbols")

# Distribution of metric values
rn_plot(result, type = "distribution")

# Stacked price + metric panel (single symbol, requires patchwork)
single <- dt[symbol == "A"]
result_a <- roll_nonlinear(single, metrics = "hurst",
                           window_size = 64L, stride = 8L)
rn_plot(result_a, type = "panel", original_data = single,
        title = "Symbol A — Hurst exponent")
}
}
