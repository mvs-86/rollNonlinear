% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/roll-by-symbol.R
\name{roll_nonlinear}
\alias{roll_nonlinear}
\title{Rolling nonlinear metrics for multiple symbols}
\usage{
roll_nonlinear(
  data,
  metrics = c("hurst", "dfa"),
  window_size = 100L,
  stride = 1L,
  series = "close",
  symbol_col = "symbol",
  time_col = "time",
  min_obs = window_size,
  metric_args = list(),
  on_error = c("warn_na", "stop", "silent_na"),
  parallel = FALSE,
  n_workers = NULL
)
}
\arguments{
\item{data}{A \code{data.table} (or \code{data.frame}, which will be coerced). Must
contain at least \code{symbol_col}, \code{time_col}, and the column(s) implied by
\code{series}.}

\item{metrics}{Character vector of metric names. Must be a subset of
\code{\link[=available_metrics]{available_metrics()}}. Default: \code{c("hurst", "dfa")}.}

\item{window_size}{Positive integer. Number of observations per window.
Default: \code{100L}.}

\item{stride}{Positive integer. Step between window anchors. Default: \code{1L}.}

\item{series}{Either a string (column name) or a function
\verb{function(dt) -> numeric} returning the series to analyse. Built-in
constructors: \code{\link[=series_close]{series_close()}}, \code{\link[=series_log_returns]{series_log_returns()}},
\code{\link[=series_typical]{series_typical()}}, \code{\link[=series_range]{series_range()}}. Default: \code{"close"}.}

\item{symbol_col}{String. Name of the symbol grouping column.
Default: \code{"symbol"}.}

\item{time_col}{String. Name of the time column. Must be strictly
increasing \emph{within each symbol}. Default: \code{"time"}.}

\item{min_obs}{Positive integer (\verb{<= window_size}). Minimum observations
required per window. Default: \code{window_size}.}

\item{metric_args}{Named list of per-metric argument overrides. Names must
be a subset of \code{metrics}. Default: \code{list()}.}

\item{on_error}{Error-handling policy passed to \code{\link[=roll_nonlinear_one]{roll_nonlinear_one()}}.
One of \code{"warn_na"} (default), \code{"stop"}, or \code{"silent_na"}.}

\item{parallel}{Logical. When \code{TRUE} and more than one symbol is present,
execution is dispatched to local \code{mirai} daemons. If \code{mirai} is not
available, falls back to sequential with a warning. Default: \code{FALSE}.}

\item{n_workers}{\code{NULL} (default) or a positive integer. Number of local
\code{mirai} daemons to start when none are already running. \code{NULL} uses
\code{parallelly::availableCores(omit = 1L)}. Ignored when \code{parallel = FALSE}
or when daemons are already running.}
}
\value{
A \code{data.table} keyed by \verb{(symbol_col, time_col)} with one row per
valid rolling window per symbol and columns:
\describe{
\item{\verb{<symbol_col>}}{Symbol identifier.}
\item{\code{time}}{Anchor time (\code{times[end]}).}
\item{\code{window_start_idx}}{Integer. Window start index.}
\item{\code{window_end_idx}}{Integer. Window end index.}
\item{\verb{<metric columns>}}{One or more numeric columns per metric.}
}
}
\description{
Computes rolling window nonlinear time-series metrics for one or more
symbols. Splits the input \code{data} by \code{symbol_col}, resolves the series via
\code{resolve_series()}, and calls \code{\link[=roll_nonlinear_one]{roll_nonlinear_one()}} on each chunk
sequentially.
}
\examples{
\donttest{
dt <- data.table::data.table(
  symbol = rep(c("A", "B"), each = 200L),
  time   = c(
    seq(as.POSIXct("2024-01-02 09:30:00", tz = "UTC"), by = 60, length.out = 200L),
    seq(as.POSIXct("2024-06-01 09:30:00", tz = "UTC"), by = 60, length.out = 200L)
  ),
  close  = c(cumsum(rnorm(200L)), cumsum(rnorm(200L)))
)
out <- roll_nonlinear(dt, metrics = "hurst", window_size = 100L, stride = 20L)
print(out)
}
}
