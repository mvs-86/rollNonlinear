% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/roll.R
\name{roll_nonlinear_one}
\alias{roll_nonlinear_one}
\title{Rolling nonlinear metrics for a single symbol}
\usage{
roll_nonlinear_one(
  x,
  times,
  metrics = c("hurst", "dfa"),
  window_size = 100L,
  stride = 1L,
  min_obs = window_size,
  on_error = c("warn_na", "stop", "silent_na")
)
}
\arguments{
\item{x}{Numeric vector. The time series values (NAs allowed in the series;
handling is per-window based on \code{on_error}).}

\item{times}{A strictly-increasing vector of timestamps corresponding to
each element of \code{x}. Must be \code{POSIXct}, \code{Date}, or integer.}

\item{metrics}{Character vector of metric names to compute. Must be a
subset of \code{\link[=available_metrics]{available_metrics()}}. Defaults to all available metrics.}

\item{window_size}{Positive integer. Number of observations per rolling
window. Default: \code{100L}.}

\item{stride}{Positive integer. Step between consecutive window anchors.
Default: \code{1L}.}

\item{min_obs}{Positive integer (<= \code{window_size}). Minimum observations
required for a window to be computed. Default: \code{window_size} (fully
formed windows only).}

\item{on_error}{Error-handling policy for metric computation failures
(including windows containing NAs):
\describe{
\item{\code{"warn_na"}}{Emit a deduplicated \code{\link[cli:cli_abort]{cli::cli_warn()}} and fill with
\code{NA_real_} (default).}
\item{\code{"stop"}}{Re-throw via \code{\link[cli:cli_abort]{cli::cli_abort()}} including window
indices.}
\item{\code{"silent_na"}}{Fill with \code{NA_real_} silently.}
}}
}
\value{
A keyed \code{data.table} (keyed by \code{time}) with one row per valid
rolling window and the following columns:
\describe{
\item{\code{time}}{Anchor time (\code{times[end]}).}
\item{\code{window_start_idx}}{Integer. 1-based start index of the window.}
\item{\code{window_end_idx}}{Integer. 1-based end index (anchor).}
\item{\code{hurst}}{Numeric. Hurst exponent (if \code{"hurst"} in \code{metrics}).}
\item{\code{dfa_alpha}}{Numeric. DFA scaling exponent (if \code{"dfa"} in
\code{metrics}).}
}
The schema is stable even when all metric values are \code{NA}. Returns a
0-row \code{data.table} with the same schema when there are no valid windows.
}
\description{
Computes rolling window nonlinear time-series metrics for a single price
(or any numeric) series using sequential execution.
}
\examples{
\donttest{
set.seed(1L)
n   <- 300L
x   <- cumsum(rnorm(n))
tms <- seq(
  as.POSIXct("2024-01-02 09:30:00", tz = "UTC"),
  by = 60,
  length.out = n
)
out <- roll_nonlinear_one(x, tms, window_size = 128L, stride = 32L)
print(out)
}
}
