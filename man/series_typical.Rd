% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transforms.R
\name{series_typical}
\alias{series_typical}
\title{Typical price: (High + Low + Close) / 3}
\usage{
series_typical(high = "high", low = "low", close = "close")
}
\arguments{
\item{high}{String. High column name. Default: \code{"high"}.}

\item{low}{String. Low column name.  Default: \code{"low"}.}

\item{close}{String. Close column name. Default: \code{"close"}.}
}
\value{
A function \verb{function(dt) -> numeric} of length \code{nrow(dt)}.
}
\description{
Returns a function that computes the typical price \code{(H + L + C) / 3}.
}
\examples{
dt <- data.table::data.table(high = c(10, 20), low = c(8, 18), close = c(9, 19))
fn <- series_typical()
fn(dt)
}
